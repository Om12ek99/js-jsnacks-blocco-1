const userNumb = parseInt(prompt("inserisci un numero"));  //prompt comando inserimento numero

// comadno for
for (i = 0; i <= userNumb; i++) {
    const cube = i * i * i;
    console.log(i, cube);
    
}